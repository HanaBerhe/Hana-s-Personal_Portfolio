{
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --outfile=dist/index.js --external:express --external:zod --external:multer --external:vite",
    
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
